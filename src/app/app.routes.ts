import { Routes } from '@angular/router';
import { CountryListComponent } from '@app/components/country-list/country-list.component';

export const routes: Routes = [{ path: '', component: CountryListComponent }];
